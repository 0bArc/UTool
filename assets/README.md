# Local assets (not in git)

**Legal:** `UnrealPak` and other engine binaries are Epic **Licensed Technology** under the [Unreal Engine EULA](https://www.unrealengine.com/en-US/eula/unreal). Do not redistribute them except as Epic permits. UTool only runs `UnrealPak.exe` if you install it locally. See [NOTICES.md](../NOTICES.md).

Place **`UnrealPak.zip`** here, or run:

```powershell
utool setup unrealpak
utool setup unrealpak --from "C:\Program Files\Epic Games\UE_5.4"
```

First use extracts to **`UnrealPak/`** (gitignored).
